<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>404 - Page Not Found</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Updated Font Awesome CDN -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body {
            background-color: white;
            color: #003366;
            font-family: 'Arial', sans-serif;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
        }
        .card {
            background-color: #f8f9fa;
            border-radius: 10px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 40px;
            text-align: center;
            max-width: 500px;
            width: 100%;
        }
        .display-4 {
            font-size: 6rem;
            font-weight: bold;
            margin-bottom: 10px;
            color: #003366;
        }
        .lead {
            font-size: 1.2rem;
            margin-bottom: 30px;
            color: #555;
        }
        .icon {
            font-size: 5rem;
            color: #003366;
            margin-bottom: 20px;
        }
        .btn-dark {
            background-color: #003366;
            border: none;
            color: white;
            padding: 12px 25px;
            text-transform: uppercase;
            font-size: 1rem;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }
        .btn-dark:hover {
            background-color: #001f4d;
        }

        /* Responsive styles */
        @media (max-width: 768px) {
            .display-4 {
                font-size: 4rem; /* Smaller font size for smaller screens */
            }
            .icon {
                font-size: 4rem; /* Adjust icon size */
            }
            .lead {
                font-size: 1rem; /* Slightly reduce text size */
            }
            .btn-dark {
                padding: 10px 20px; /* Adjust button padding */
                font-size: 0.9rem; /* Adjust button font size */
            }
            .card {
                padding: 30px; /* Adjust card padding */
            }
        }

        @media (max-width: 576px) {
            .display-4 {
                font-size: 3rem; /* Further reduction for very small screens */
            }
            .icon {
                font-size: 3rem; /* Further adjust icon size */
            }
            .lead {
                font-size: 0.9rem; /* Reduce text size */
            }
            .btn-dark {
                padding: 8px 18px; /* Smaller button padding */
                font-size: 0.85rem; /* Smaller button text */
            }
            .card {
                padding: 20px; /* Smaller padding for the card */
            }
        }
    </style>
</head>
<body>
    <div class="container d-flex justify-content-center align-items-center">
        <div class="card">
            <i class="fas fa-exclamation-triangle icon"></i>
            <h1 class="display-4">Error 404!</h1>
            <p class="lead">Oops! You're lost. The page you are looking for cannot be found.</p>
            <a href="/#" class="btn btn-dark">Home</a>
        </div>
    </div>

    <!-- Bootstrap JS & Dependencies -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
