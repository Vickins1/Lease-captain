<!DOCTYPE html>
<html lang="en">

<head>
       <meta charset="UTF-8">
       <meta name="viewport" content="width=device-width, initial-scale=1.0">
       <title>Contact Support</title>
       <link rel="stylesheet" href="/path/to/your/styles.css">
</head>

<body>
       <div class="container mt-5">
              <h1 class="text-center text-primary">Contact Support</h1>
              <p class="text-center">If you need assistance, please fill out the form below. Our support team will get
                     back to you as soon as possible.</p>

              <!-- Display flash messages -->
              <% if (successMessage && successMessage.length> 0) { %>
                     <div class="alert alert-success">
                            <%= successMessage %>
                     </div>
                     <% } %>

                            <% if (errorMessage && errorMessage.length> 0) { %>
                                   <div class="alert alert-danger">
                                          <%= errorMessage %>
                                   </div>
                                   <% } %>

                                          <!-- Support Form -->
                                          <form id="supportForm" action="/support/submit" method="POST"
                                                 class="support-form">
                                                 <div class="form-group">
                                                        <label for="emailAddress">Your Email</label>
                                                        <input type="email" name="emailAddress" class="form-control"
                                                               id="emailAddress" placeholder="Enter your email"
                                                               required>
                                                 </div>
                                                 <div class="form-group">
                                                        <label for="supportMessage">Message</label>
                                                        <textarea name="supportMessage" class="form-control"
                                                               id="supportMessage" rows="4"
                                                               placeholder="Describe your issue... or Request Demo" required></textarea>
                                                 </div>
                                                 <div class="d-flex justify-content-between">
                                                        <button type="button" onclick="window.history.back()"
                                                               class="btn btn-secondary">Go Back</button>
                                                        <button type="submit" class="btn btn-primary">Send
                                                               Message</button>
                                                 </div>
                                          </form>
       </div>

       <!-- Floating Home Button -->
       <div class="floating-home-button" onclick="window.location.href='/'">
              <i class="fas fa-home"><strong><<</strong></i>
       </div>


       <!-- Styles -->
       <style>
              /* Container styling */
              .container {
                     max-width: 800px;
                     margin: 40px auto;
                     padding: 30px;
                     box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
                     border-radius: 8px;
                     background-color: #f9f9f9;
              }

              /* Title styling */
              h1.text-primary {
                     color: #003366;
                     font-size: 2rem;
                     font-weight: bold;
                     margin-bottom: 10px;
              }

              /* Centered text styling */
              .text-center {
                     text-align: center;
                     color: #555;
              }

              /* Flash message styling */
              .alert {
                     padding: 15px;
                     margin-bottom: 20px;
                     border-radius: 5px;
                     font-size: 0.9rem;
              }

              .alert-success {
                     background-color: #d4edda;
                     color: #155724;
                     border: 1px solid #c3e6cb;
              }

              .alert-danger {
                     background-color: #f8d7da;
                     color: #721c24;
                     border: 1px solid #f5c6cb;
              }

              /* Form styling */
              .form-group {
                     margin-bottom: 15px;
              }

              .form-control {
                     width: 100%;
                     padding: 10px;
                     border: 1px solid #ccc;
                     border-radius: 5px;
                     font-size: 1rem;
              }

              /* Button styling */
              .btn-primary,
              .btn-secondary {
                     padding: 10px 20px;
                     border: none;
                     border-radius: 5px;
                     font-size: 1rem;
                     cursor: pointer;
                     transition: background-color 0.3s;
              }

              .btn-primary {
                     background-color: #003366;
                     color: #ffffff;
              }

              .btn-primary:hover {
                     background-color: #0056b3;
              }

              .btn-secondary {
                     background-color: #6c757d;
                     color: #ffffff;
              }

              .btn-secondary:hover {
                     background-color: #5a6268;
              }

              /* Floating Home Button Styling */
              .floating-home-button {
                     position: fixed;
                     bottom: 20px;
                     right: 20px;
                     width: 60px;
                     height: 60px;
                     background-color: #003366;
                     color: white;
                     border-radius: 50%;
                     display: flex;
                     align-items: center;
                     justify-content: center;
                     font-size: 1.5rem;
                     cursor: pointer;
                     box-shadow: 0px 4px 12px rgba(0, 0, 0, 0.2);
                     transition: background-color 0.3s, transform 0.3s;
                     z-index: 1000;
              }

              .floating-home-button:hover {
                     background-color: #0056b3;
                     transform: scale(1.1);
              }

              /* Adjustments for the icon */
              .floating-home-button i {
                     font-size: 1.5rem;
              }

              /* Responsive adjustments */
              @media (max-width: 576px) {
                     .floating-home-button {
                            width: 50px;
                            height: 50px;
                            font-size: 1.2rem;
                     }

                     .floating-home-button i {
                            font-size: 1.2rem;
                     }
              }


              /* Responsive adjustments */
              @media (max-width: 768px) {
                     h1.text-primary {
                            font-size: 1.8rem;
                     }

                     .form-control {
                            font-size: 0.9rem;
                     }

                     .btn-primary,
                     .btn-secondary {
                            font-size: 0.9rem;
                            padding: 8px 16px;
                     }

                     .container {
                            padding: 15px;
                     }
              }

              @media (max-width: 576px) {
                     h1.text-primary {
                            font-size: 1.5rem;
                     }

                     .form-control {
                            font-size: 0.85rem;
                            padding: 8px;
                     }

                     .btn-primary,
                     .btn-secondary {
                            font-size: 0.85rem;
                            padding: 6px 12px;
                     }

                     .container {
                            padding: 10px;
                     }
              }
       </style>
</body>

</html>